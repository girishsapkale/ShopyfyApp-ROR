<%= form_tag('/homes/update_metals', method: :put) do %>
<h3>Listing Metals</h3>
<table id="products" class="table table-striped table-bordered" cellspacing="0" width="90%">
  <thead>
    <tr>
      <th>Metal title</th>
      <th>Metal price</th>
    </tr>
  </thead>
  <tbody>
    <% @variants.each do |variant| %>
    <tr>
      <td><%= variant.metal_title %></td>
      <td><%= text_field_tag 'metal_prices[]', variant.metal_price %></td>
        <%= hidden_field_tag 'ids[]', variant.id %>      
    </tr>
    <% end %>
    <tr><td></td>
      <td><%= submit_tag 'Save Changes' , class: 'btn btn-danger' %></td>
    </tr>
  </tbody>
</table>
  
<% end %>
<%#= link_to 'New Variant', new_variant_path, class: 'btn btn-info' %> 

<!-- <h3>Shopify Base URL</h3>
<table id="products" class="table table-striped table-bordered" cellspacing="0" width="90%">
  <thead>
    <tr>
      <th>Shop URL</th>
      <th>Action</th>
    </tr>
  </thead>
  <tbody>
    <%# @users.each do |user| %>
    <tr>
      <td><%#= Home.first.shop_url if Home.first %></td>  
      <td><%#= link_to 'Edit', edit_home_path(Home.first) if Home.first %>
        <%#= link_to 'Add new one', new_home_path if !Home.first.present? %></td>    
      <td><%#= link_to 'Change Variant Price', edit_variant_path(product), {style: "color: blue"} %></td>
       <td><%#= link_to 'Show', variant %></td>
      <td><%#= link_to 'Edit', edit_variant_path(variant) %></td>
      <td><%#= link_to 'Destroy', variant, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
    <!-- </tr>
    <%# end %>
  </tbody>
</table>
<%#= link_to 'New Variant', new_variant_path, class: 'btn btn-info' if !Variant.first %>

 --> 